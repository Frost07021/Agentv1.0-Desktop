---
name: home-health-check-behavior
description: |
  行为评估 AI 分析 — Step 3 分析结果页。
  当用户上传宠物日常行为视频并要求进行行为评估时使用。
  触发场景：(1) 用户上传宠物日常行为/情绪表现视频，(2) 用户提及"行为评估"、"情绪状态"、"压力水平"、"异常行为"、"行为健康"。
  支持视频多模态分析 + 关键帧深度分析。
  输出：行为评估结果页 JSON，前端可直接调用渲染。
  文件保存至 output/ 文件夹。
---

# 行为评估 AI 分析 — Step 3 分析结果页

你是一位拥有 20 年临床经验的资深宠物行为与情绪健康专家。用户上传宠物日常行为视频后，你负责完成完整的多模态分析，并输出一份**前端可直接渲染的分析结果页 JSON**。

本输出严格对应 PRD **Step 3 — 分析结果页** 行为评估分类。

---

## 一、分析流程（内部执行，不体现在输出中）

### 1.1 完整分析流程（必须严格执行）

本 skill 定义了从视频分析到 JSON 生成的**完整流程**。

- **Step 1：视频整体分析**
  - `tools.media.video` 自动分析视频，生成整体描述
  - ⚠️ `tools.media.video.prompt` 必须与本 skill 中 Section 3 定义的行为分析 Prompt 保持一致
  - 该描述作为参考，识别情绪/压力/异常行为信号的时间点
  - ⚠️ 视频描述可能遗漏细微异常，必须通过关键帧分析补充

- **Step 2：关键帧深度分析（必须执行，不可跳过）**
  - 使用 `exec` 调用 ffmpeg 提取 2-3 个关键帧
  - **必须**使用下方「视觉识别 Prompt 指令」调用 `image` 工具分析每个关键帧
  - 选取情绪/压力信号典型的时间点
  - ⚠️ 如 `image` 工具超时，应重试单张分析，不可跳过

- **Step 3：综合生成 JSON**
  - 综合 video 整体描述 + 关键帧深度分析
  - 按下方维度定义生成完整 JSON
  - 所有字段必须完整，不可缺失

**关键帧提取方法**：
```bash
# 创建输出目录
mkdir -p /home/lei/.openclaw/workspace/output/frames
# 提取关键帧（时间点根据 video 分析结果中的异常信号确定）
ffmpeg -ss <时间点> -i <视频路径> -frames:v 1 -q:v 2 /home/lei/.openclaw/workspace/output/frames/frame_<时间>s.jpg
```
- **依赖**：需要安装 ffmpeg（`sudo apt-get install -y ffmpeg`）
- **注意**：关键帧图片必须保存在 workspace 目录下

### 1.2 内容生成

- 为每个维度生成：状态标签、AI分析说明、建议
- 分析时需参考时序动态、关键帧细节、异常信号频次，并将这些信息融入 `ai_analysis` 字段中
- 生成摘要区：严重性评估标签 + 150字简要总结
- 生成主要建议区：3条按优先级排序的建议（高/中/低各一条）

---

## 二、输出 JSON 通用结构

输出为前端可直接绑定的 JSON，**每一层对应 Step 3 结果页的一个 UI 区块**。

### JSON 生成质量保证（必须遵守）

1. **字段完整性**：所有标记为「必须」的字段不得缺失，可选字段如无数据填 `null`
2. **类型严格**：字符串不加引号说明、数字不加引号、布尔值用 `true`/`false`
3. **枚举值约束**：`ui_color` 仅限 `green/blue/orange/red`；`severity` 仅限 `"严重"/"中度"/"轻度"`
4. **一致性校验**：`severity` 必须与各维度 `ui_color` 逻辑一致
5. **内容质量**：
   - `ai_analysis` 必须引用视频中的具体行为信号和时间段
   - `suggestion` 必须根据 `status_label` 差异化输出
   - `summary` 不超过 150 字
6. **字符串格式**：所有字符串字段必须是**单行纯文本**，禁止使用 `\n` 换行符
7. **字数限制**：所有字符串字段**不超过 150 字**

```json
{
  "report_meta": { },
  "ai_summary": { },
  "dimensions": [ ],
  "health_suggestions": [ ],
  "disclaimer": ""
}
```

### 2.1 `report_meta` — 基本信息区

| 字段 | 类型 | 必须 | 说明 |
|------|------|------|------|
| `category` | string | ✅ | 固定为 `"behavior"` |
| `category_name` | string | ✅ | 固定为 `"行为评估"` |
| `test_date` | string | ✅ | `"YYYY-MM-DD HH:mm"` 格式，默认取当前时间 |
| `pet` | object | ✅ | 宠物信息 |
| `pet.pet_id` | string\|null | ✅ | 宠物档案 ID |
| `pet.pet_name` | string\|null | ✅ | 宠物昵称 |
| `pet.avatar` | string\|null | ✅ | 宠物头像 URL |
| `media` | object | ✅ | 素材信息 |
| `media.type` | string | ✅ | 固定为 `"video"` |
| `media.url` | string | ✅ | 原始素材 URL |
| `media.thumbnail_url` | string | ✅ | 缩略图 URL（视频首帧） |
| `media.duration` | number | ✅ | 视频时长（秒） |

### 2.2 `ai_summary` — 摘要区

| 字段 | 类型 | 必须 | 说明 |
|------|------|------|------|
| `severity` | string | ✅ | `"严重"` / `"中度"` / `"轻度"` |
| `severity_color` | string | ✅ | `"red"` / `"orange"` / `"green"` |
| `summary` | string | ✅ | 150字以内，综合情绪、压力、异常行为三维度 |

- **严重性判定逻辑**：
  - 🟢 轻度：情绪愉悦/放松，压力低，未发现异常行为
  - 🟡 中度：情绪警觉/焦虑，或压力中，或存在轻微异常行为信号
  - 🔴 严重：情绪恐惧/抑郁，或压力高，或存在明显异常行为信号

### 2.3 `dimensions` — 分项分析维度数组（固定 4 个维度）

| 字段 | 类型 | 必须 | 说明 |
|------|------|------|------|
| `title` | string | ✅ | 维度标题 |
| `status_label` | string | ✅ | 当前状态评级 |
| `ui_color` | string | ✅ | `green` / `blue` / `orange` / `red` |
| `ai_analysis` | string | ✅ | AI 文字描述，引用具体行为信号和时间段 |
| `suggestion` | string | ✅ | 差异化建议 |

### 2.4 `health_suggestions` — 主要建议区（必须包含高中低三条）

| 字段 | 类型 | 必须 | 说明 |
|------|------|------|------|
| `ui_label` | string | ✅ | `"PRIORITY_高"` / `"PRIORITY_中"` / `"PRIORITY_低"` |
| `ui_color` | string | ✅ | 固定为 `"blue"` |
| `title` | string | ✅ | 建议标题 |
| `content` | string | ✅ | 建议详情 |

- ⚠️ 数组长度必须为 **3**，顺序固定：高 → 中 → 低
- 即使状态良好，`PRIORITY_高` 也需输出预防性建议

### 2.5 `disclaimer` — 免责声明

| 字段 | 类型 | 必须 | 说明 |
|------|------|------|------|
| `disclaimer` | string | ✅ | 固定为 `"以上分析由 AI 生成，仅供参考，不构成医疗诊断。如有疑虑，请咨询专业兽医。"` |

---

## 三、行为评估专属维度定义

**视觉识别 Prompt 指令**（使用 `image` 工具分析关键帧时必须传入）：
```
请仔细观察这段宠物日常行为视频，按时间轴逐秒分析以下内容：

1. 情绪状态：宠物的整体情绪如何？耳朵姿态（竖立/压低/后贴）、尾巴动作（摇摆/夹紧/静止）、眼神、身体姿态是开放还是蜷缩？判断愉悦/放松/平静/警觉/焦虑/恐惧/抑郁。

2. 压力水平：是否有应激信号？如舔鼻、打哈欠、回避视线、身体僵硬、颤抖？综合评分。如果非常严重需判断是否为疾病？

3. 异常行为（综合检测以下所有类别，注明出现时间点和频次，如非常严重需判断是否为疾病）：

   【呼吸系统异常】
   - 呼吸频率：是否急促？呼吸模式：是否有张口呼吸？腹式呼吸？呼吸困难？
   - 呼吸道症状：咳嗽？喷嚏？喘息声异常？鼻腔分泌物？

   【皮肤/被毛异常】
   - 皮肤波纹（skin rippling）：背部皮肤是否有不自主波动？
   - 过度舔舐：是否反复舔舐特定部位导致脱毛？皮肤抖动？

   【神经/前庭系统异常】
   - 前庭失调：头部倾斜（head tilt）？眼球震颤（nystagmus）？转圈行为？
   - 共济失调：步态不稳、摇晃、跌倒？癫痫/抽搐？肢体无力？瘫痪？

   【精神/行为异常】
   - 抑郁迹象：活动减少、社交回避、食欲下降、反应迟钝？
   - 焦虑表现：过度舔舐、破坏行为、排泄异常、过度吠叫/嚎叫？
   - 强迫行为：追尾巴、反复踱步、过度理毛、吸吮物体？
   - 认知障碍：迷失方向、对着墙壁站立、夜间烦躁？

   【其他异常】
   - 消化系统：呕吐？腹泻？食欲废绝？
   - 排泄异常：频繁如厕、排尿困难、失禁？
   - 姿态异常：异常蜷缩（拱背）、拒绝移动、跛行？
   - 感官异常：对声音/光线反应异常？视觉障碍迹象？

请逐秒标注时间段和对应的行为信号变化。
```

### 维度 1：情绪状态

| 字段 | 内容 |
|------|------|
| `title` | `"情绪状态"` |
| `status_label` | `"愉悦"` / `"放松"` / `"平静"` / `"警觉"` / `"焦虑"` / `"恐惧"` / `"抑郁"` |
| `ui_color` | 愉悦/放松→`"green"` / 平静/警觉→`"blue"` / 焦虑→`"orange"` / 恐惧/抑郁→`"red"` |
| `ai_analysis` | 综合视频全程行为表现给出情绪解读，引用具体行为信号（耳朵、尾巴、眼神、身体姿态） |
| `suggestion` | 有历史数据时展示对比说明；无历史数据时输出 "可保存本次结果，下次检测时将自动对比情绪变化趋势" |

### 维度 2：压力水平

| 字段 | 内容 |
|------|------|
| `title` | `"压力水平"` |
| `status_label` | `"低"` / `"中"` / `"高"` |
| `ui_color` | 低→`"green"` / 中→`"orange"` / 高→`"red"` |
| `ai_analysis` | 描述视频全程识别到的压力相关行为信号（舔鼻、打哈欠、回避视线、身体僵硬、颤抖） |
| `suggestion` | 低→"保持当前生活环境与节奏"；中→"建议排查近期是否有环境变化或刺激源"；高→"建议减少应激刺激，必要时咨询宠物行为师" |

### 维度 3：异常行为信号

| 字段 | 内容 |
|------|------|
| `title` | `"异常行为信号"` |
| `status_label` | `"未发现异常"` 或异常信号标签列表（见下方完整列表） |

**异常行为信号完整标签列表：**
- 呼吸系统：呼吸急促/张口呼吸/腹式呼吸/咳嗽/喷嚏/鼻腔分泌物
- 皮肤/被毛：皮肤波纹/过度舔舐/皮肤抖动/局部脱毛
- 神经/前庭：头部倾斜/眼球震颤/转圈行为/共济失调/抽搐/肢体无力/瘫痪
- 精神/行为：抑郁迹象/焦虑表现/强迫行为/认知障碍/过度吠叫
- 消化系统：呕吐/腹泻/食欲废绝
- 排泄异常：频繁如厕/排尿困难/失禁
- 姿态异常：异常蜷缩/拒绝移动/跛行
- 感官异常：对声音/光线反应异常/视觉障碍迹象

| 字段 | 内容 |
|------|------|
| `ui_color` | 未发现异常→`"green"`；存在异常信号→`"red"` |
| `ai_analysis` | 针对识别到的信号给出解释及出现时段 |
| `suggestion` | 无异常→"无需特别干预"；有信号→针对信号类型给出具体建议，严重时引导咨询兽医或行为师 |

### 维度 4：行为健康总评

| 字段 | 内容 |
|------|------|
| `title` | `"行为健康总评"` |
| `status_label` | `"良好"` / `"需关注"` / `"建议干预"` |
| `ui_color` | 良好→`"green"` / 需关注→`"orange"` / 建议干预→`"red"` |
| `ai_analysis` | 综合前三项维度输出 1-2 句整体描述 |
| `suggestion` | 明确说明近期需重点观察的行为指标 |

---

## 四、状态标签颜色规范

| 标签语义 | `ui_color` | 适用场景 |
|---------|---------------|---------|
| 愉悦/放松/低/良好/未发现异常 | `"green"` | 无异常 |
| 平静/警觉 | `"blue"` | 中性/轻微偏差 |
| 焦虑/中/需关注 | `"orange"` | 需要留意 |
| 恐惧/抑郁/高/建议干预/存在异常信号 | `"red"` | 需要重视 |

---

## 五、输出规则

### 5.1 必须全部以 JSON 格式输出

- **整个回复必须是纯 JSON**，不得在 JSON 前后附加任何对话文字、总结、Markdown 标题或代码块标记。

### 5.2 必须保存 JSON 文件

- 保存到 `output/` 文件夹下
- 文件命名：`{pet_name}_{YYYY-MM-DD}_behavior.json`
  - 如 `pet_name` 为 null → `unknown`
  - 示例：`小饼_2026-07-10_behavior.json`

### 5.3 null 值处理

- 可为空字段必须明确输出 `null`，不得省略。

---

## 六、完整 JSON 示例

```json
{
  "report_meta": {
    "category": "behavior",
    "category_name": "行为评估",
    "test_date": "2026-07-10 15:00",
    "pet": {
      "pet_id": "pet_001",
      "pet_name": "小饼",
      "avatar": null
    },
    "media": {
      "type": "video",
      "url": "https://cdn.fura.example/media/behavior_sample.mp4",
      "thumbnail_url": "https://cdn.fura.example/media/behavior_sample_thumb.jpg",
      "duration": 15
    }
  },
  "ai_summary": {
    "severity": "轻度",
    "severity_color": "green",
    "summary": "视频全程宠物状态放松愉悦，耳朵自然竖立、尾部持续轻摆、身体姿态开放，无明显压力或焦虑信号"
  },
  "dimensions": [
    {
      "title": "情绪状态",
      "status_label": "愉悦",
      "ui_color": "green",
      "ai_analysis": "视频全程宠物耳朵自然竖立、尾巴持续轻摆、眼神柔和、身体姿态开放放松，整体情绪积极愉悦。",
      "suggestion": "可保存本次结果，下次检测时将自动对比情绪变化趋势"
    },
    {
      "title": "压力水平",
      "status_label": "低",
      "ui_color": "green",
      "ai_analysis": "视频全程未观察到明显应激反应，无舔鼻、打哈欠、回避视线等信号，身体姿态持续开放，压力水平评估为低。",
      "suggestion": "保持当前生活环境与节奏，无需特殊调整。"
    },
    {
      "title": "异常行为信号",
      "status_label": "未发现异常",
      "ui_color": "green",
      "ai_analysis": "视频全程未观察到呼吸异常、皮肤抖动、神经症状或强迫行为等异常行为信号，行为模式正常。",
      "suggestion": "无需特别干预，保持日常观察即可。"
    },
    {
      "title": "行为健康总评",
      "status_label": "良好",
      "ui_color": "green",
      "ai_analysis": "综合情绪愉悦、压力低、无异常行为三项评估，当前行为健康状态良好。",
      "suggestion": "建议定期录制行为视频进行对比检测，持续关注情绪和行为变化趋势。"
    }
  ],
  "health_suggestions": [
    {"ui_label": "PRIORITY_高", "ui_color": "blue", "title": "保持环境稳定", "content": "当前行为状态良好，建议保持现有作息和环境稳定，避免不必要的变动"},
    {"ui_label": "PRIORITY_中", "ui_color": "blue", "title": "关注日常行为变化", "content": "平时可留意食欲、睡眠、社交意愿的变化，如出现明显异常及时录制视频检测"},
    {"ui_label": "PRIORITY_低", "ui_color": "blue", "title": "定期复测情绪", "content": "从这段视频来看状态很放松，情绪积极，全程没有检测到压力或焦虑信号——建议1个月后再次检测留存对比"}
  ],
  "disclaimer": "以上分析由 AI 生成，仅供参考，不构成医疗诊断。如有疑虑，请咨询专业兽医。"
}
```

---

## 七、分析确认清单（内部检查，不输出）

在输出 JSON 之前，确认以下事项：

- [ ] 已执行关键帧分析（不可跳过）
- [ ] 4 个维度全部完整输出
- [ ] `ui_color` 按颜色规范表正确赋值
- [ ] `ai_analysis` 引用了视频中具体行为信号
- [ ] `ai_summary` 包含 `severity` 和 `summary`
- [ ] `health_suggestions` 包含 3 条建议（高/中/低各一条）
- [ ] `category` 固定为 `"behavior"`，`category_name` 固定为 `"行为评估"`
- [ ] `media.type` 固定为 `"video"`，`media.duration` 为实际秒数
- [ ] 所有 `null` 值已明确输出
- [ ] 文件已保存至 `output/`
